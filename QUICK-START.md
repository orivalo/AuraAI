# Быстрый старт Aura AI

## ✅ Что уже настроено

1. **Страница логина** (`/login`) - с кнопкой "Войти через Google"
2. **Middleware** - автоматически защищает все роуты
3. **Авторизация** - требуется для доступа к чату
4. **Интеграция с Supabase** - сохранение сообщений и настроения

## 🔐 Как работает авторизация

1. Пользователь открывает приложение → автоматически перенаправляется на `/login`
2. Нажимает "Войти через Google" → перенаправление на Google
3. После авторизации → возврат на `/api/auth/callback`
4. Callback создает сессию → перенаправление на главную страницу
5. Теперь пользователь может использовать чат

## 📝 Переменные окружения

Убедитесь, что в `.env.local` есть:

```env
# Groq
GROQ_API_KEY=your_groq_key

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

## 🚀 Запуск

```bash
npm run dev
```

Откройте http://localhost:3000 - вас автоматически перенаправит на `/login`

## 🔧 Настройка Google OAuth

Если еще не настроено:

1. **Google Cloud Console**:
   - Создайте OAuth 2.0 Client ID
   - Добавьте redirect URI: `https://your-project.supabase.co/auth/v1/callback`

2. **Supabase Dashboard**:
   - Authentication → Providers → Google
   - Включите и вставьте Client ID и Secret
   - Сохраните

3. **URL Configuration**:
   - Site URL: `http://localhost:3000` (для разработки)
   - Redirect URLs: `http://localhost:3000/api/auth/callback`

## ✨ Готово!

После настройки просто откройте приложение и войдите через Google.

